import assert from 'node:assert/strict';
const {chromium}=await import(process.env.PLAYWRIGHT_MODULE||'playwright');
const browser=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'});
try{const page=await browser.newPage({viewport:{width:1200,height:900}}),errors=[];page.on('pageerror',e=>errors.push(e.message));await page.route('**/harness',r=>r.fulfill({contentType:'text/html',body:'<link rel="stylesheet" href="/spatial.css"><div id="stage" style="width:850px"><svg id="ties"></svg><div id="nodes"></div></div>'}));await page.goto((process.env.PEOPLE_TEST_ORIGIN||'http://127.0.0.1:4184')+'/harness');await page.evaluate(async()=>{const {Scene}=await import('/scene.mjs');const {sceneData}=await import('/model.mjs');window.build=count=>{const people=Array.from({length:count},(_,i)=>({id:'p'+i,name:'Person '+i}));window.data=sceneData({},people.map(node=>({node})));window.scene??=new Scene(document.querySelector('#stage'),n=>{window.picked=n.id;scene.set(data,[],n.id)});scene.set(data,[],null);};build(554);});assert.equal(await page.locator('.orb').count(),554);await page.locator('.orb').last().click();assert.equal(await page.evaluate(()=>picked),'p553');await page.screenshot({path:'/tmp/all-people-554.png'});await page.evaluate(()=>build(1500));assert.equal(await page.locator('.orb').count(),1500);assert.ok(await page.evaluate(()=>scene.elements.every(e=>{const x=parseFloat(e.style.getPropertyValue('--point-x')),y=parseFloat(e.style.getPropertyValue('--point-y'));return x>0&&x<850&&y>0&&y<510;})));await page.evaluate(()=>{scene.rotate(.25);scene.scale(.2);scene.reset()});await page.locator('.orb').last().click();assert.equal(await page.evaluate(()=>picked),'p1499');await page.keyboard.press('Tab');await page.locator('.orb').first().focus();assert.equal(await page.locator('.orb').first().locator('.name').evaluate(e=>getComputedStyle(e).visibility),'visible');assert.deepEqual(errors,[]);console.log('PASS: 554 and 1500 nodes, fitted positions, last-person selection, orbit/zoom/reset.');}finally{await browser.close();}
